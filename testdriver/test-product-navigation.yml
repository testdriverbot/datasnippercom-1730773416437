version: 4.0.80
steps:
  - prompt: Click on "Products" in the header menu.
    commands:
      - command: hover-text
        text: Products
        description: link in the header
        action: click
  - prompt: Click on "Products" in the header menu.
    commands:
      - command: hover-text
        text: Products
        description: link in the header
        action: click
  - prompt: Assert that a dropdown or products page appears.
    commands:
      - command: hover-text
        text: Products
        description: link in the header
        action: click
